{
  "name": "propulse-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "db:migrate:local": "wrangler d1 migrations apply propulse-analytics --local",
    "db:migrate:remote": "wrangler d1 migrations apply propulse-analytics --remote",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
